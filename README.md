# DynamicColumns
Example of columns that will resize dynamically with the screen resize.

Choose how many columms do you want to have in page from 1 to 12.
The style is a similar to how Bootstrap works with columns and screen size, using JQuery to dynamically add and remove columns.
